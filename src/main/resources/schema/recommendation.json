{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://campus-food-recommend/schemas/recommendation.json",
  "title": "Recommendation",
  "description": "AI 推荐接口的返回结构(F-3 阶段使用)",
  "type": "object",
  "required": ["merchantId", "reason", "confidence"],
  "additionalProperties": false,
  "properties": {
    "merchantId": {
      "type": "array",
      "minItems": 1,
      "maxItems": 20,
      "items": {
        "type": "string",
        "pattern": "^[A-Za-z0-9][A-Za-z0-9-]{0,40}$"
      },
      "description": "推荐商户 ID 列表,按 confidence 降序(F-16.2 polish:放宽到任意字母数字短横线,兼容 EVM-XXX / m-001 / M-NOODLE 等格式)"
    },
    "reason": {
      "type": "string",
      "minLength": 1,
      "maxLength": 500
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1
    }
  }
}